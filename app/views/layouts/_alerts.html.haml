.container
  - flash.each do |msg_type, msg|
    %div{class: "alert alert-info alert-dismissible fade show alert-top bold"}
      = msg
      %button.close{"aria-label" => "Close", "data-dismiss" => "alert", type: "button"}
        %span{"aria-hidden" => "true"} &times;